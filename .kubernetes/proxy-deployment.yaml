apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: proxy
spec:
  replicas: 2
  minReadySeconds: 15
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: proxy
    spec:
      containers:
        - image: jeroenvdheuvel/kubernetes-helm-test:proxy
          imagePullPolicy: Always
          name: werkspot-kubernetes-helm-proxy
          ports:
            - containerPort: 8080
